---
name: "react"
description: "React (via `vike-react`) — the UI library this app renders with. Use when writing or debugging components, hooks (`useState`/`useEffect`/`useContext`), refs, context, or fixing SSR/hydration mismatches."
---

Up-to-date React documentation for this project. Read it before working with React:

https://react.dev/llms.txt
